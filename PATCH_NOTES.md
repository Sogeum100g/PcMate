# PcMate 업데이트 패치 노트

이번 업데이트에서는 캐릭터 상태와 모니터링 대상을 확장하고, 한국어·영어 지원과 도움말, 클릭 상호작용을 추가했습니다. 기존 기능의 사용성과 표시 안정성도 함께 개선했습니다.

## 주요 변경 사항

### 1. Sleeping 상태 추가

- 캐릭터 상태가 `Sleeping → Standing → Walking → Running`의 네 단계로 확장되었습니다.
- 커스텀 캐릭터 등록 및 수정 화면에서 Sleeping 이미지를 별도로 지정할 수 있습니다.
- 커스텀 캐릭터 이미지 입력 순서를 Sleeping, Standing, Walking, Running으로 통일했습니다.
- Sleeping 이미지가 없던 기존 커스텀 캐릭터는 Standing 이미지를 대신 사용해 계속 불러올 수 있습니다.

### 2. 네트워크 리소스 모니터링 추가

- 메모리, CPU, GPU에 이어 Network 리소스를 선택할 수 있습니다.
- Windows 네트워크 인터페이스의 누적 수신 바이트를 바탕으로 실제 수신 속도를 Mbps 단위로 계산합니다.
- 링크 속도나 별도의 속도 테스트가 아니라 PC에서 이미 발생 중인 트래픽을 측정합니다.
- 측정을 위해 추가 네트워크 트래픽을 생성하거나 데이터를 사용하지 않습니다.
- 네트워크 전용 상태 경계값을 Mbps 단위로 설정할 수 있습니다.

### 3. 한국어·영어 언어팩 지원

- 한국어와 영어 UI 언어팩을 추가했습니다.
- 기본값은 Windows 시스템 언어를 따릅니다.
- 우클릭 메뉴의 `Language / 언어`에서 시스템 기본값, English, 한국어를 직접 선택할 수 있습니다.
- 선택한 언어는 저장되며 메뉴, 도움말, 설정 창, 캐릭터 관리 창, 오류 메시지와 오버레이 문구에 함께 적용됩니다.

### 4. 온보딩 도움말 추가

- 최초 실행 시 PcMate의 주요 사용법을 안내하는 도움말이 표시됩니다.
- 캐릭터 이동과 클릭, 우클릭 메뉴, 리소스 선택, 상태 경계값, 커스텀 캐릭터 등록 및 네트워크 측정 방식을 한 화면에서 확인할 수 있습니다.
- 이후에도 우클릭 메뉴의 `Help / Tutorial` 또는 `도움말 / 튜토리얼`에서 다시 열 수 있습니다.
- 도움말 창의 크기를 자유롭게 조절할 수 있습니다.

### 5. 캐릭터 클릭 상호작용 추가

- 캐릭터를 왼쪽 클릭하면 짧은 인사말이 말풍선으로 표시됩니다.
- 클릭과 드래그를 Windows 기본 드래그 거리 기준으로 구분해, 클릭할 때 캐릭터가 의도치 않게 움직이는 현상을 줄였습니다.
- 기존 드래그 이동 기능은 그대로 사용할 수 있습니다.

## 사용성 및 표시 개선

- 말풍선이 텍스트 길이에 맞춰 자동으로 크기를 조절하도록 변경했습니다.
- 말풍선을 닫으면 창 크기와 위치가 원래 상태로 복원됩니다.
- 다크 모드에서 리소스 글자를 흰색으로 표시하고, 밝은 배경에서는 어두운 글자를 사용할 수 있도록 대비를 개선했습니다.
- 메모리·CPU·GPU는 `%`, 네트워크는 `Mbps`로 리소스 바와 경계값 단위를 구분해 표시합니다.

## 참고

- 네트워크 모니터링은 다운로드·스트리밍 등 현재 PC에서 실제로 수신 중인 트래픽이 있을 때 높게 표시됩니다.
