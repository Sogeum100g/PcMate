# PcMate 향후 확장 계획

이 문서는 `PcMate_PLAN.md`에서 분리한 미래 구현 후보 목록이다.

현재 구현 목표는 1차 MVP에 한정한다. 아래 항목은 MVP가 안정적으로 동작한 뒤 검토할 확장 방향이며, 현재 단계의 필수 구현 범위가 아니다.

---

## 1. 리소스 확장

- CPU 사용률
- GPU 사용률
- 디스크 사용률
- 네트워크 송수신량
- 프로세스별 메모리 사용량 TOP 5
- 프로세스별 CPU 사용량 TOP 5
- 온도 센서

---

## 2. 시스템 모니터링 도구화

- 특정 프로세스 감시
- 과도한 메모리 사용 프로세스 표시
- 앱별 리소스 사용량 리포트
- 로그 저장
- 리소스 사용량 히스토리 그래프

---

## 3. 보안 도구 방향 확장

- 파일 변경 이벤트 감시
- 짧은 시간 내 대량 파일 수정 감지
- 의심 프로세스 알림
- 랜섬웨어 행위 탐지 실험
- 이벤트 로그 기반 간단한 리포트 생성

---

## 4. 캐릭터 기능 확장

- 상태별 대사
- 투명도 조절
- 마우스 반응
- 드래그 반응
- 스킨 교체
- 사용자 캐릭터 에셋 등록 설정 창
- `character.json` 기반 캐릭터 매니페스트 로딩
- 외부 캐릭터 폴더 등록 및 검증
- 등록된 캐릭터 목록을 우클릭 메뉴에 동적 반영
- Live2D 또는 Spine 적용

### 4.1 사용자 에셋 등록 창

MVP 이후 사용자가 임의로 추가한 캐릭터 에셋을 설정 창에서 등록할 수 있게 한다.

기본 방향:

- 캐릭터 폴더마다 `character.json` 매니페스트를 둔다.
- 매니페스트에는 캐릭터 id, 표시 이름, 상태별 애니메이션 폴더, 파일 prefix, 확장자, 프레임 간격을 정의한다.
- 설정 창에서 캐릭터 폴더를 선택하면 매니페스트와 프레임 파일 존재 여부를 검증한다.
- 검증에 성공한 외부 폴더 경로는 `%LOCALAPPDATA%/PcMate/characters.json`에 저장한다.
- 등록 직후 우클릭 메뉴의 캐릭터 목록을 갱신한다.
- 잘못된 폴더, 누락된 상태 이미지, 지원하지 않는 확장자는 사용자에게 명확히 안내한다.

---

## 5. AI 기능 확장

1차 MVP에서는 제외하지만, 추후 would 기능으로 고려한다.

- 현재 PC 상태를 자연어로 설명
- 사용자가 "왜 얘가 뛰고 있어?"라고 물으면 리소스 사용량 설명
- 리소스 사용량이 높은 프로세스 요약
- 초보자용 해결 가이드 제공
- 시스템 상태 리포트 자동 생성

---

## 6. 확장 착수 기준

다음 조건을 만족한 뒤 이 문서의 항목을 하나씩 현재 계획으로 승격한다.

- 1차 MVP 완료 기준을 모두 충족한다.
- 메모리 모니터링, 상태 분류, UI 표시 계층이 분리되어 있다.
- 새 리소스 모니터를 추가해도 기존 UI와 상태 분류 흐름을 크게 흔들지 않는다.
- 확장 항목마다 사용자에게 보여줄 가치와 완료 기준이 명확하다.
